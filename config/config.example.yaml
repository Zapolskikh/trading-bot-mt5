app:
  mode: "paper"            # "live" | "paper" | "backtest"
  symbols: ["US500.cash", "US100.cash", "US30.cash"]
  base_timeframe: "M1"
  data_window: 500

risk:
  per_trade_pct: 0.5
  per_day_pct: 2.0
  max_active_trades: 4
  dynamic:
    enabled: false

metatrader:
  login:
  password:
  server: FTMO-Demo

telegram:
  enabled: true

journal:
  path: "./journal"
  rotate_daily: true
