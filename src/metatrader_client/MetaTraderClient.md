# MetaTrader5 Python Client - High Level Design (HLD)

## Обзор

`MetaTraderClient` — это полнофункциональная обёртка над MetaTrader5 Python API, предоставляющая упрощённый интерфейс для:
- Подключения к MT5 терминалу
- Получения рыночных данных (OHLCV, тиковые данные)
- Управления ордерами (выставление, модификация, отмена)
- Управления позициями (открытие, закрытие)
- Получения статуса счёта и портфеля

## Архитектура

```
┌─────────────────────────────────────────┐
│   Приложение / Trading Bot / Strategy   │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│   MetaTraderClient (wrapper)            │
│  - Connection Management                │
│  - Data Access                          │
│  - Order Management                     │
│  - Position Management                  │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│   MetaTrader5 Python API                │
│  (Dynamic import for flexibility)       │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│   MT5 Terminal / TradingServer          │
│  (FTMO, Broker, Demo Account)           │
└─────────────────────────────────────────┘
```

## Компоненты

### 1. Инициализация и конфигурация

**Класс:** `MetaTraderClient(config: Dict[str, Any])`

**Параметры конфигурации:**
```python
{
    "login": int,  # Номер счёта (обязателен)
    "password": str,  # Пароль (обязателен)
    "server": str,  # Сервер (обязателен, например "FTMO-Demo")
}
```

**Состояние:**
- `self.connected: bool` — флаг соединения
- `self.login_info: Dict` — информация об аккаунте

---

### 2. Управление соединением

#### `connect(login=None, password=None, server=None, portable=True) -> bool`

**Назначение:** Подключение к MT5 терминалу

**Логика:**
1. Динамическая загрузка модуля MetaTrader5
2. Инициализация терминала с логином, паролем, сервером
3. Валидация статуса терминала и аккаунта
4. Сохранение информации о логине

**Возвращает:** `True` если успешно, `False` иначе

**Примечание:** Параметры метода переопределяют config, если передан не None.

---

#### `disconnect() -> None`

**Назначение:** Закрытие соединения с терминалом и очистка состояния клиента

**Логика:**
1. Вызов `mt5.shutdown()` — закрытие соединения с терминалом
2. Сброс флага `self.connected`
3. Очистка `self.login_info`

**Важно:** Метод **НЕ закрывает GUI окно MetaTrader5**. Это является стандартным поведением MT5 Python API по следующим причинам:
- Архитектурное разделение между API и GUI
- Терминал может использоваться для ручной торговли
- Пользователь сохраняет контроль над процессом терминала

---

#### `is_connected() -> bool`

**Назначение:** Проверка статуса соединения

**Логика:**
1. Проверка наличия модуля
2. Проверка `mt5.terminal_info()` и `mt5.account_info()`
3. Возвращает `True` только если оба не None

---

#### `get_status() -> Dict[str, Any]`

**Назначение:** Получить полный статус подключения и счёта

**Возвращает:**
```python
{
    "connected": bool,
    "login": int,
    "server": str,
    "balance": float,
    "equity": float,
    "build": int,  # версия терминала
}
```

---

### 3. Получение рыночных данных

#### `get_market_data(symbol: str, timeframe: str, window: int) -> pd.DataFrame`

**Назначение:** Загрузка исторических OHLCV данных

**Параметры:**
- `symbol`: "EURUSD", "GBPUSD", etc.
- `timeframe`: "M1", "M5", "M15", "M30", "H1", "H4", "D1", "W1", "MN1"
- `window`: количество свечей (например 100)

**Возвращает:** pandas DataFrame с индексом по времени:
```
                     open      high       low     close  tick_volume
time
2026-01-09 15:00  1.08234  1.08345  1.08123  1.08234       1876
2026-01-09 16:00  1.08345  1.08456  1.08234  1.08345       5999
```

**Колонки:** `open`, `high`, `low`, `close`, `tick_volume`, `real_volume`, `spread`

**Использование:**
- Совместим с **pandas-ta** для технического анализа
- Совместим с **ta-lib** (numpy arrays)
- Поддерживает стандартные pandas операции (rolling, groupby, shift и т.д.)

**Примеры:**
```python
# pandas-ta
df.ta.rsi(length=14)
df.ta.macd()

# ta-lib
import talib

rsi = talib.RSI(df["close"].values, timeperiod=14)
```

---

#### `get_tick(symbol: str) -> Dict[str, Any]`

**Назначение:** Получить текущий тик (последние bid/ask цены)

**Возвращает:**
```python
{
    "time": datetime,  # Время обновления
    "bid": float,  # Цена предложения (покупка)
    "ask": float,  # Цена запроса (продажа)
    "last": float,  # Последняя торговая цена
    "volume": int,  # Объём тика
    "spread": float,  # Спред (ask - bid)
}
```

**Применение:**
- Проверка текущей цены перед входом
- Расчёт маржи и риска
- Валидация цены ордера

---

### 4. Управление ордерами (РЕАЛИЗОВАНО)

#### `place_order(symbol, side, volume, order_type="market", price=None, sl=None, tp=None, volume_currency="lots") -> Dict`

**Назначение:** Выставить торговый ордер (рыночный или отложенный)

**Параметры:**
- `symbol`: "EURUSD", "USDCHF", etc.
- `side`: "buy" или "sell"
- `volume`: размер сделки (интерпретация зависит от volume_currency)
- `order_type`: "market" (немедленное исполнение) или "limit" или "stop"
- `price`: цена для limit/stop ордеров
- `sl`: цена Stop Loss (опционально)
- `tp`: цена Take Profit (опционально)
- `volume_currency`: **Единица измерения объёма:**
  - `"lots"` (default) — объём в лотах (0.1, 0.5, 1.0 и т.д.)
  - `"usd"` — объём в долларах США (конвертируется в лоты через contract_size)
  - `"eur"` — объём в евро (конвертируется через EUR/USD, затем в лоты)

**Конвертация объёма:**
```
Если volume_currency="usd":
  lots = volume / contract_size
  Пример: 1000 USD на EURUSD (contract_size=100000) → 0.01 лот

Если volume_currency="eur":
  usd = volume * eur_usd_rate
  lots = usd / contract_size
  Пример: 1000 EUR на EURUSD (rate=1.08) → 1080 USD → 0.0108 лот
```

**Логика выставления:**
1. Конвертация объёма в лоты (если volume_currency != "lots")
2. Валидация параметров и минимальных объёмов
3. Определение типа действия:
   - Рыночный: `TRADE_ACTION_DEAL` с `ORDER_FILLING_FOK`
   - Отложенный (limit/stop): `TRADE_ACTION_PENDING` с `ORDER_FILLING_IOC`
4. Формирование `MqlTradeRequest`
5. Отправка через `mt5.order_send()`

**Возвращает:**
```python
{
    "success": bool,  # Успешность операции
    "ticket": int,  # ID ордера/сделки
    "volume": float,  # Исполненный объём (в лотах)
    "price": float,  # Цена исполнения
    "comment": str,  # Комментарий сервера
    "retcode": int,  # Код результата (0=успех)
    "action": str,  # Описание действия
}
```

**Примеры:**
```python
# Рыночный ордер на 1000 USD
order = client.place_order(
    symbol="EURUSD", side="buy", volume=1000, volume_currency="usd"
)

# Лимит ордер на 0.1 лота
order = client.place_order(
    symbol="EURUSD",
    side="buy",
    volume=0.1,
    order_type="limit",
    price=1.08000,
    volume_currency="lots",
)

# Стоп ордер на 500 EUR
order = client.place_order(
    symbol="EURUSD",
    side="sell",
    volume=500,
    order_type="stop",
    price=1.09000,
    volume_currency="eur",
    sl=1.09500,
    tp=1.07500,
)
```

---

#### `modify_order(order_id: int, sl=None, tp=None, price=None) -> Dict`

**Назначение:** Модифицировать параметры активного ордера

**Параметры:**
- `order_id`: ticket ордера для изменения
- `sl`: новая цена Stop Loss
- `tp`: новая цена Take Profit
- `price`: новая цена триггера (для limit/stop)

**Логика:**
1. Получение информации текущего ордера через `mt5.orders_get(ticket=order_id)`
2. Сохранение старых значений
3. Формирование `TRADE_ACTION_MODIFY` запроса
4. Отправка через `mt5.order_send()`

**Возвращает:**
```python
{
    "success": bool,
    "ticket": int,
    "retcode": int,
    "comment": str,
    "old_values": {"price": float, "sl": float, "tp": float},
    "new_values": {"price": float, "sl": float, "tp": float},
}
```

---

#### `cancel_order(order_id: int) -> Dict`

**Назначение:** Отменить активный отложенный ордер

**Параметры:**
- `order_id`: ticket ордера для отмены

**Логика:**
1. Получение ордера через `mt5.orders_get(ticket=order_id)`
2. Формирование `TRADE_ACTION_REMOVE` запроса
3. Отправка через `mt5.order_send()`

**Возвращает:**
```python
{
    "success": bool,
    "ticket": int,
    "retcode": int,
    "comment": str,
}
```

---

#### `get_orders() -> List[Dict[str, Any]]`

**Назначение:** Получить список всех активных (отложенных) ордеров

**Логика:**
1. Вызов `mt5.orders_get()`
2. Преобразование каждого ордера в словарь
3. Маппинг типов ордеров в строки ("buy_limit", "sell_limit", etc.)

**Возвращает:**
```python
[
    {
        "ticket": int,  # ID ордера
        "symbol": str,  # Символ
        "type": str,  # "buy_limit", "sell_limit", "buy_stop", "sell_stop"
        "state": str,  # Состояние
        "volume": float,  # Объём в лотах
        "price": float,  # Цена триггера
        "sl": float,  # Stop Loss
        "tp": float,  # Take Profit
        "time_setup": datetime,  # Время создания
        "comment": str,  # Комментарий
        "magic": int,  # ID советника
    },
    ...,
]
```

---

### 5. Управление позициями (TODO)

#### `close_position(position_id: str, lots=None) -> str`

**Статус:** 🔴 **TODO** — В процессе разработки

**Назначение:** Закрытие открытой позиции полностью или частично

**Планируемая реализация:**
- Получение информации о позиции через `mt5.positions_get()`
- Формирование противоположного market order
- Использование `TRADE_ACTION_DEAL` с `ORDER_FILLING_FOK`
- Возврат deal_id закрывающей сделки

---

#### `get_positions() -> List[Dict[str, Any]]`

**Статус:** 🔴 **TODO** — В процессе разработки

**Назначение:** Получить список открытых позиций

**Планируемая реализация:**
- Получение позиций через `mt5.positions_get()`
- Преобразование в структуру словарей
- Включение информации: символ, объём, цена входа, P&L, маржа

---

### 6. История и статистика (TODO)

#### `get_history(since=None, until=None) -> List[Dict[str, Any]]`

**Статус:** 🔴 **TODO** — В процессе разработки

**Назначение:** Получить историю закрытых сделок за период

**Планируемая реализация:**
- Парсинг дат (since/until)
- Получение истории через `mt5.history_deals_get()`
- Преобразование в словари с временем открытия/закрытия, P&L

---

#### `get_portfolio() -> Dict[str, Any]`

**Статус:** 🔴 **TODO** — В процессе разработки

**Назначение:** Получить ключевые метрики портфеля

**Планируемая реализация:**
```python
{
    "balance": float,  # Баланс счёта
    "equity": float,  # Эквити
    "margin": float,  # Используемая маржа
    "free_margin": float,  # Свободная маржа
    "margin_level": float,  # Уровень маржи (%)
}
```

---

### 7. Справочная информация

#### `get_symbol_info(symbol: str) -> Dict[str, Any]`

**Назначение:** Получить параметры торговой пары

**Возвращает:**
```python
{
    "symbol": str,  # Наименование
    "digits": int,  # Знаков после запятой (5 для EURUSD)
    "point": float,  # Размер пункта (0.00001 для EURUSD)
    "contract_size": float,  # Размер лота в единицах (100000 для EURUSD)
    "lot_step": float,  # Минимальный шаг (0.01)
    "min_lot": float,  # Минимальный лот (0.01)
    "max_lot": float,  # Максимальный лот
    "tick_value": float,  # Стоимость пункта
    "tick_size": float,  # Минимальное движение цены
    "spread": float,  # Спред в пунктах
    "ask": float,  # Текущая цена ask
    "bid": float,  # Текущая цена bid
}
```

---

#### `usd_to_lots(amount_usd: float, symbol: str) -> float`

**Назначение:** Конвертировать сумму в USD в объём в лотах

**Логика:**
1. Получить `contract_size` символа
2. Рассчитать: `lots = amount_usd / contract_size`
3. Округлить до `lot_step`
4. Проверить минимум `min_lot`

**Пример:** 1000 USD на EURUSD (contract_size=100000) → 0.01 лота

---

#### `eur_to_lots(amount_eur: float, symbol: str) -> float`

**Назначение:** Конвертировать сумму в EUR в объём в лотах

**Логика:**
1. Получить текущий курс EUR/USD через `get_tick("EURUSD")`
2. Конвертировать: `amount_usd = amount_eur * eur_usd_rate`
3. Применить логику `usd_to_lots()`

---

#### `last_error() -> Tuple[int, str]`

**Назначение:** Получить последнюю ошибку MT5

**Возвращает:** `(error_code: int, error_message: str)`

---

## Обработка ошибок

**Статус ошибок (retcode) MT5:**

| Код | Значение | Действие |
|-----|----------|---------|
| 10009 | TRADE_RETCODE_DONE | Успех ✅ |
| 10015 | TRADE_RETCODE_PRICE_OFF | Неверная цена ❌ |
| 10017 | TRADE_RETCODE_INVALID_STOPS | Неверные SL/TP ❌ |
| 10019 | TRADE_RETCODE_TRADE_DISABLED | Торговля отключена ❌ |
| 10030 | TRADE_RETCODE_UNSUPPORTED_FILLING | Режим заполнения не поддерживается ❌ |

---

## Примеры использования

### Пример 1: Подключение и получение статуса

```python
from metatrader_client.client import MetaTraderClient

config = {
    "login": 1234567,
    "password": "Password123",
    "server": "FTMO-Demo",
}

client = MetaTraderClient(config)

if client.connect():
    status = client.get_status()
    print(f"Connected: {status['login']}, Equity: {status['equity']}")
else:
    print("Connection failed")
```

### Пример 2: Получение рыночных данных

```python
# Загрузить 100 часовых баров EURUSD
df = client.get_market_data("EURUSD", "H1", 100)
print(f"Loaded {len(df)} bars")
print(df.head())

# Использовать с pandas-ta
df.ta.rsi()  # Добавить RSI
df.ta.macd()  # Добавить MACD
```

### Пример 3: Выставление и управление ордером

```python
# Выставить BUY LIMIT на 1000 USD
order = client.place_order(
    symbol="USDCHF",
    side="buy",
    volume=1000.0,
    order_type="limit",
    price=0.79500,
    volume_currency="usd",
)

if order["success"]:
    ticket = order["ticket"]
    print(f"Order placed: {ticket}")

    # Модифицировать ордер
    modify = client.modify_order(order_id=ticket, price=0.79600, sl=0.79400, tp=0.79800)

    # Отменить ордер
    cancel = client.cancel_order(ticket)
```

---

## Состояние реализации

| Компонент | Статус | Примечание |
|-----------|--------|-----------|
| Подключение | ✅ Готово | Полная реализация |
| Рыночные данные | ✅ Готово | DataFrame с pandas-ta |
| Выставление ордеров | ✅ Готово | Market + Pending |
| Модификация ордеров | ✅ Готово | Price, SL, TP |
| Отмена ордеров | ✅ Готово | Удаление из очереди |
| Получение ордеров | ✅ Готово | Полный список активных |
| Закрытие позиций | 🔴 TODO | Разработка в процессе |
| Получение позиций | 🔴 TODO | Разработка в процессе |
| История сделок | 🔴 TODO | Разработка в процессе |
| Метрики портфеля | 🔴 TODO | Разработка в процессе |

---
